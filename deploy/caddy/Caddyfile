{
  # Use {$VAR} — expandido ao carregar o Caddyfile (variáveis do container Docker).
  email {$CADDY_EMAIL}
}

{$DOMAIN}, www.{$DOMAIN} {
  encode gzip

  # Painel, /mesa/, /api/ e /uploads/: nginx no container `web` repassa /api ao `api`.
  reverse_proxy web:80
}

