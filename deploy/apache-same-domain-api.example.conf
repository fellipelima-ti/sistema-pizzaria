# Dentro do VirtualHost do site (HTTPS), encaminha /api/* para o Node.
# Exige: a2enmod proxy proxy_http
#
# ProxyPreserveHost On
# ProxyPass        /api/ http://127.0.0.1:3333/
# ProxyPassReverse /api/ http://127.0.0.1:3333/
#
# Front build: VITE_API_URL=/api
