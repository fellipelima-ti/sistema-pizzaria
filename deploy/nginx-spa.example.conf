# Exemplo Nginx: frontend (Vite build) + fallback SPA para /mesa/...
# Ajuste paths, server_name e SSL.
#
# O build do React deve estar em: /var/www/pizzaria/dist
# Certificados: certbot certonly --nginx -d seudominio.com
#
# server {
#   listen 443 ssl http2;
#   server_name seudominio.com www.seudominio.com;
#
#   root /var/www/pizzaria/dist;
#   index index.html;
#
#   ssl_certificate     /etc/letsencrypt/live/seudominio.com/fullchain.pem;
#   ssl_certificate_key /etc/letsencrypt/live/seudominio.com/privkey.pem;
#
#   # API no mesmo dominio (opcional): descomente e aponte para o Node
#   # location /api/ {
#   #   proxy_pass http://127.0.0.1:3333/;
#   #   proxy_http_version 1.1;
#   #   proxy_set_header Host $host;
#   #   proxy_set_header X-Real-IP $remote_addr;
#   #   proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#   #   proxy_set_header X-Forwarded-Proto $scheme;
#   # }
#
#   # Arquivos estaticos do Vite (assets com hash)
#   location /assets/ {
#     try_files $uri =404;
#     add_header Cache-Control "public, max-age=31536000, immutable";
#   }
#
#   # React Router: /mesa/TOKEN, / (painel), etc.
#   location / {
#     try_files $uri $uri/ /index.html;
#   }
# }
#
# server {
#   listen 80;
#   server_name seudominio.com www.seudominio.com;
#   return 301 https://$host$request_uri;
# }
